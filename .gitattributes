# すべてのテキストを LF に正規化（WSL/CIで安定）
* text=auto eol=lf

# 代表的テキスト拡張子は明示（念のため）
*.py      text eol=lf
*.md      text eol=lf
*.toml    text eol=lf
*.yaml    text eol=lf
*.yml     text eol=lf
*.json    text eol=lf
*.ini     text eol=lf
*.cfg     text eol=lf
*.csv     text eol=lf
*.sh      text eol=lf

# バイナリは改行変換しない
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.ico     binary
*.pdf     binary
*.zip     binary
*.7z      binary
*.exe     binary
*.dll     binary

# 競合しやすい生成物は、基本コミットしない方針（.gitignore側で除外）
# どうしても必要な場合だけ、LFSや配布用Releaseで管理する

# --- (任意) Git LFS を使う場合の例。必要になったらコメント解除 ---
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text

# --- (任意) アーカイブ作成時に含めないファイル（GitHub Releaseのソースtar用） ---
# .github export-ignore
# tests/ export-ignore
# .pre-commit-config.yaml export-ignore
