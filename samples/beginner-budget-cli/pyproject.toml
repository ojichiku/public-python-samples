[project]
name = "beginner-budget-cli"
version = "0.1.0"
description = "A beginner-friendly Python CLI sample for calculating a simple monthly budget."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
