# 段階的な実装計画

各段階で動作を確認してから次へ進みます。

1. [x] 基盤: Python 3.14、uv、srcレイアウト、CLI入口を用意する。
2. [x] Edge起動: 同期APIでインストール済みEdgeを画面表示付きで起動・終了する。
3. [x] 検索操作: GitHubの検索画面を開き、検索欄への入力とEnterを実装する。
4. [x] 結果取得: 実DOMを確認し、上位10件と0件を判別する。
5. [x] CSV保存: 5列のヘッダー、BOM、欠損値、保存エラーを扱う。
6. [x] 検証・文書化: 自動テスト、実Edgeでの確認、READMEを完成させる。

## 検証記録

- 作業開始時: REQUEST.mdを確認。Python 3.14はインストール済み。
- 2026-09-23: Python 3.14.7、Playwright 1.63.0、pytest 9.1.1で検証。
- 実EdgeのGitHub画面で検索欄、結果カード、説明、言語、Star数、0件表示を確認。
- pytestへ移行し、単体・実Edge DOMテスト計15件が成功。
- 実Edgeでplaywright pythonの10件取得、CSVのBOM・ヘッダー・Star数、および0件検索でヘッダーのみのCSVを確認。
- 追加要望: 操作間に1〜3秒のランダム待機を追加。表示完了待機とは分離し、自動リトライは行わない。
- 最終検証: `uv run pytest --run-edge -q` は16件成功。`uv run pytest -q` は11件成功・Edge用5件スキップ。
- ランダム待機追加後も実GitHubで10件と0件の検索・CSV保存が成功。ブロック防止の効果は保証しない。
- Edge未導入、通信障害、保存権限エラーはモックで検証。実際の組織ポリシーやアクセス制限下での動作は未検証。
