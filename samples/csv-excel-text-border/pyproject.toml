[project]
name = "csv-excel-text-border"
version = "0.1.0"
description = "Convert CSV files to Excel while preserving values as text and adding borders."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
]

[tool.uv]
package = false
