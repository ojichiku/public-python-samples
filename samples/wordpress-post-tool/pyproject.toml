[project]
name = "wordpress-post-tool"
version = "0.1.0"
description = "Post a Markdown draft to WordPress with the REST API."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.10.2",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]

[tool.uv]
package = false
