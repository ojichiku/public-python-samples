[project]
name = "wp-rest-api-connection-check"
version = "0.1.0"
description = "Check WordPress REST API authentication with an application password."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]

[tool.uv]
package = false
