# Default logging configuration shared across Python samples.
level: INFO

stdout:
  enabled: true

file:
  enabled: true
  path: logs/app.log
  rotation: null
  max_files: 5

format: "%(asctime)s.%(msecs)03d [%(levelname)s] %(message)s"
date_format: "%Y/%m/%d %H:%M:%S"
